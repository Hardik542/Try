#include <stdio.h>
#include <stdlib.h>

// defining to initiate structure

struct node{
    int data;
    struct node *ptr;
}

// Creating the function to add the node and its data
void insert_elemet(struct node** head,int data);

void main(){
    
    // defining the struct here 
    struct node head;

}

// defining the function here
void insert_element(struct node** head,int data){
    
}